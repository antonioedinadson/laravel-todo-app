<div class="input-area-item">
    <label for="{{ $id }}">{{ $label }}</label>
    <input placeholder="{{ $place ?? '' }}" value="{{ $value ?? '' }}" type="{{ $type }}"
        name="{{ $name }}" id="{{ $id }}">
</div>
